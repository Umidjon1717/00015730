#include <stdio.h>
#include <string.h>
#include <ctype.h>

int main(){
    
     //1-misol

    // char ism[] = "Umidjon";
    // char fam[] = "Izzatullayev";

    // for (int i = 0, j = 1; i < strlen(fam) && j < strlen(ism); i += 2, j += 2) {
    //     char temp = ism[j];
    //     ism[j] = fam[i];
    //     fam[i] = temp;
    // }

    // printf("%s\n", ism);
    // printf("%s\n", fam);

    // return 0;






    //2-misol

    // char gap[30]="";
    // scanf("%[^\n]s", gap);
    // int l=strlen(gap);
    // for(int i=0; gap[i]; i++){
    //    if(islower(gap[0])){
    //      gap[0]=toupper(gap[0]);
    //    }else if(gap[l]!='.'){
    //     gap[l]='.';
    //    }
    // }
    // puts(gap);





    //3-misol

    // char gap[30];
    // scanf("%[^\n]s", gap);
    // int count=0;
    // for(int i=0; i<strlen(gap); i++){
    //     for(int k=0; gap[k]; k++){
    //         if(gap[i]==gap[k]){
    //            count++;
    //         }
    //     }printf("%c-> %d ta", gap[i], count);
    //     count=0;
    //     printf("\n");
    // }





    //4-misol

    // char belgi[50];
    // scanf("%[^\n]s", belgi);
    // int count=0;

    // for(int i=0; i<strlen(belgi); i++){
    //     if(isalpha(belgi[i])|| isdigit(belgi[i])|| isspace(belgi[i])){
            
    //     }else count++;
    // }
    // printf("spets belgilar soni: %dta", count);








    //5-misol

    // char gap[50];
    // printf("Kiriting: ");
    // scanf("%[^\n]s", gap);

    // for(int i=0; gap[i]; i++){
    //     printf("%c", gap[i]);
    //     if(isspace(gap[i])){
    //         printf("\n");
    //     }
    // }






    //6-misol

    // char gap[50];
    // printf("Kiriting; ");
    // scanf("%[^\n]s", gap);

    // for(int i=0; i<strlen(gap); i++){
    //     if(gap[i]=='o'&&gap[i+1]!='\''){
    //         printf("o'");
    //     }else if(gap[i]=='o'&&gap[i+1]=='\''){
    //         printf("o''");
    //     }else printf("%c", gap[i]);
    // }
    











}

