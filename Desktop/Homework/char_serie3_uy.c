#include <stdio.h>
#include <string.h>
#include<ctype.h>


int main(){

    //4-masala

    // char uzun[100];
    // char new[100]="";
    // printf("Kiriting: ");
    // scanf("%[^\n]s", uzun);
    
    // char *ptr=strtok(uzun, " ");

    // while(ptr!=NULL){
    //     if(strlen(ptr)>strlen(new)){
    //         strcpy(new, ptr);
    //     }
    //     ptr=strtok(NULL, " " );
        
    // }
    // puts(new);






    //5-misol

    // char gap[100];
    // printf("Kiriting: ");
    // scanf("%[^\n]s", gap);
    // int count=0; 

    // for(int i=0; i<=strlen(gap); i++){
    //     if(gap[i]=='\0'){
    //         for(int j=i-1; j>=0; j--){
    //             if(gap[j]!=' ') count++;
    //             else break;
    //         }
    //     }
    // }
    // printf("Oxirgi so'z uzunligi -> %d", count);






    //6-misol

    // char gap[100];
    // printf("Kiriting: ");
    // scanf("%[^\n]s", gap);

    // char *ptr=strtok(gap, " ");

    // while(ptr!=NULL){
    //     for(int i=strlen(ptr)-1; i>=0; i--){
    //     printf("%c", ptr[i]);
    //     }printf(" ");
    //     ptr=strtok(NULL, " ");
    // }

}
